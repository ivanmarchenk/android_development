<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomTheme" parent="Theme.AppCompat.Light">
        <item name="android:textColor">#FF0000</item>
        <item name="android:textSize">30sp</item>
    </style>

    <style name="MyTextStyle" parent="MyTextStyle" tools:ignore="ResourceCycle">
        <item name="android:textColor">#546e7a</item>
        <item name="android:textSize">25sp</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="MatrixInputWindowStyle" parent="MatrixInputWindowStyle" tools:ignore="ResourceCycle">
        <item name="android:textAlignment">center</item>
        <item name="android:layout_width">53dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:ems">10</item>
        <item name="android:inputType">numberSigned|numberDecimal</item>
    </style>

</resources>